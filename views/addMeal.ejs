<!DOCTYPE html>
<html>
<head>
  <title>Add Meal</title>
  <link rel="stylesheet" href="/style.css">
  <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: 'Roboto', sans-serif;
      background: #f9f9f9;
      margin: 0;
      padding: 0;
      display: flex;
      flex-direction: column;
      align-items: center;
    }

    header {
      background: #ff7043;
      width: 100%;
      padding: 20px 0;
      color: white;
      text-align: center;
      box-shadow: 0 4px 6px rgba(0,0,0,0.2);
    }

    .card {
      background: white;
      padding: 40px 50px;
      border-radius: 12px;
      box-shadow: 0 8px 20px rgba(0,0,0,0.15);
      margin-top: 50px;
      width: 400px;
    }

    h2 {
      text-align: center;
      color: #333;
    }

    form {
      display: flex;
      flex-direction: column;
      gap: 15px;
      margin-top: 20px;
    }

    label {
      font-weight: 700;
      margin-bottom: 5px;
    }

    input[type="text"], textarea, select {
      padding: 10px;
      border-radius: 6px;
      border: 1px solid #ccc;
      font-size: 14px;
      width: 100%;
    }

    textarea {
      resize: vertical;
    }

    button {
      background: #ff7043;
      color: white;
      border: none;
      padding: 12px;
      border-radius: 8px;
      font-size: 16px;
      cursor: pointer;
      transition: background 0.2s, transform 0.2s;
    }

    button:hover {
      background: #ff5722;
      transform: translateY(-2px);
    }

    .success-msg {
      color: green;
      text-align: center;
      margin-bottom: 15px;
      font-weight: 600;
    }

    .links {
      display: flex;
      justify-content: space-between;
      margin-top: 20px;
    }

    .links a {
      text-decoration: none;
      color: #ff7043;
      font-weight: 600;
    }

    .links a:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>

  <header>
    <h2>🍲 Add a Meal</h2>
  </header>

  <div class="card">

    <% if (success) { %>
      <p class="success-msg"><%= success %></p>
    <% } %>

    <form action="/add-meal" method="POST">
      <label>Meal Name:</label>
      <input type="text" name="mealName" placeholder="e.g., Sweet Potato Curry" required>

      <label>Ingredients (comma separated):</label>
      <textarea name="ingredients" placeholder="e.g., Sweet potato, salt, pepper" required></textarea>

      <label>Time (Breakfast, Lunch, Dinner):</label>
      <select name="time">
        <option>Breakfast</option>
        <option>Lunch</option>
        <option>Dinner</option>
      </select>

      <button type="submit">Add Meal</button>
    </form>

    <div class="links">
      <a href="/meals">View All Meals</a>
      <a href="/">Home</a>
    </div>

  </div>

</body>
</html>
