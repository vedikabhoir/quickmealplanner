<!DOCTYPE html>
<html>
<head>
  <title>Shopping List</title>
  <link rel="stylesheet" href="/style.css">
  <style>
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background-color: #f5f5f5;
      margin: 0;
      padding: 20px;
    }
    h2 {
      color: #2c3e50;
      text-align: center;
    }
    a {
      text-decoration: none;
      color: #2980b9;
      margin-right: 15px;
      font-weight: bold;
    }
    a:hover {
      color: #e74c3c;
    }
    .container {
      max-width: 700px;
      margin: auto;
      background-color: #fff;
      padding: 25px;
      border-radius: 10px;
      box-shadow: 0 4px 8px rgba(0,0,0,0.1);
    }
    ul {
      list-style: none;
      padding: 0;
    }
    li {
      background-color: #ecf0f1;
      margin: 8px 0;
      padding: 12px 15px;
      border-radius: 8px;
      font-size: 18px;
      color: #2c3e50;
      transition: background-color 0.2s;
    }
    li:hover {
      background-color: #d0d7de;
    }
    hr {
      border: none;
      height: 1px;
      background-color: #bdc3c7;
      margin: 20px 0;
    }
  </style>
</head>
<body>
  <div class="container">
    <h2>🛒 Weekly Shopping List</h2>
    <div style="text-align: center;">
      <a href="/weekly-plan">Back to Weekly Plan</a> | 
      <a href="/">Home</a>
    </div>
    <hr>

    <% if (uniqueIngredients.length === 0) { %>
      <p style="text-align:center; color:#e74c3c;">No ingredients found!</p>
    <% } else { %>
      <ul>
        <% uniqueIngredients.forEach(item => { %>
          <li><%= item %></li>
        <% }) %>
      </ul>
    <% } %>
  </div>
</body>
</html>
